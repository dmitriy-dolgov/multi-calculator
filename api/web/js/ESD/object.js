/**
 * Общий объект.
 */

gl.objectMSD = function (nameObj) {
    //var name = name;

    return {
        getName: function () {
            return nameObj;
        },

        setEvent: function (nameStr) {

        }
    }
};
